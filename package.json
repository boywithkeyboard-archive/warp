{
  "name": "@boywithkeyboard/warp",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=cjs --platform=node --outfile=build/index.js",
    "check": "tsc"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "cdn-cgi-trace": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.5",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.4"
  }
}
